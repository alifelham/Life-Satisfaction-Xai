# Published results and figures

- `published_metrics.json` contains the classical-model results reported in the Heliyon article.
- `figures/` contains the study figures available with the implementation.
- `figure_manifest.json` records figure checksums, byte sizes, and corresponding notebook cells.

Analysis outputs are written to `results/generated/`.
